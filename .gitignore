# Cache files
cache/*.cache

# Local config (contains DB credentials — do NOT commit)
# Uncomment if you want to protect config from accidental commit:
# config/config.php

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# Install lock (generated after setup)
install/installed.lock
